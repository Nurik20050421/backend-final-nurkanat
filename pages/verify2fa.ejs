<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify OTP</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Axios CDN -->
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
</head>
<body class="bg-black text-white">
    <div class="container d-flex justify-content-center align-items-center vh-100">
        <div class="card shadow-lg p-4" style="max-width: 500px; width: 100%; background-color: #000000;">
            <!-- Error Message -->
            <div id="error-message" class="alert alert-danger text-center" style="display: none;"></div>

            <!-- OTP Verification Content -->
            <h2 class="text-center mb-4" style="color: #F8C000;">Verify OTP for <%= username %></h2>
            <p class="text-center mb-4 text-muted">Enter the 6-digit code from your authentication app.</p>

            <!-- OTP Form -->
            <form id="verifyForm">
                <div class="mb-3">
                    <label for="otp" class="form-label" style="color: white;">OTP</label>
                    <input type="text" id="otp" name="token" class="form-control" maxlength="6" style="background-color: #333333; color: white; border: 1px solid #F8C000;" required>
                </div>
                <button type="submit" class="btn" style="background-color: #F8C000; color: black; width: 100%;">Verify</button>
            </form>

            <div class="mt-3 text-center">
                <a href="/" class="text-muted" style="color: #F8C000;">Back to login</a>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        const username = "<%= username %>";
    </script>
    <script src="js/verify-otp.js"></script>
</body>
</html>
